

const AnimalPic = ({animalURL}) => {

    return (
        <img 
        src={animalURL} 
        alt="the picture didn't manage to load"
        id="shibu-pic"></img>
    )
}

export default AnimalPic